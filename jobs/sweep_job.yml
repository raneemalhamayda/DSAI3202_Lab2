$schema: https://azuremlschemas.azureedge.net/latest/sweepJob.schema.json
type: sweep

display_name: amazon_review_sweep
experiment_name: assignment2_manual_training

trial:
  code: ../src
  command: >-
    python train.py
    --train_data ${{inputs.train_data}}
    --val_data ${{inputs.val_data}}
    --test_data ${{inputs.test_data}}
    --deploy_data ${{inputs.deploy_data}}
    --model_output ${{outputs.model_output}}
    --C ${{search_space.C}}
    --max_iter ${{search_space.max_iter}}
  environment:
    image: mcr.microsoft.com/azureml/openmpi4.1.0-ubuntu20.04
    conda_file: ../env/conda.yaml

inputs:
  train_data:
    type: uri_folder
    path: azureml:amazon_review_merged_features_train@latest
  val_data:
    type: uri_folder
    path: azureml:amazon_review_merged_features_val@latest
  test_data:
    type: uri_folder
    path: azureml:amazon_review_merged_features_test@latest
  deploy_data:
    type: uri_folder
    path: azureml:amazon_review_merged_features_deploy@latest

outputs:
  model_output:
    type: uri_folder

compute: azureml:cpu-cluster

sampling_algorithm: random

search_space:
  C:
    type: uniform
    min_value: 0.01
    max_value: 10.0
  max_iter:
    type: choice
    values: [500, 1000, 2000]

objective:
  primary_metric: val_accuracy
  goal: maximize

limits:
  max_total_trials: 6
  max_concurrent_trials: 2
